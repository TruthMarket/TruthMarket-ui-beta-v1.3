export interface MinterIncomeType {
    tokenId:number,
    income:number,
}

export interface OrderMoneyType {
    tokenId:number,
    orderMoney:number,
}

export interface PayType {
    tokenId:number,
    fee:number,
}