export interface Uint256_text {
    uint256:number,
    text:string,
}

export interface Uint256_bools {
    uint256:number,
    bools:boolean,
}

export interface Address_bools {
    address:string,
    bools:boolean,
}

export interface NFTBoxType {
    address:string,
    bools:boolean,
}

export interface Uint256_bytes {
    uint256:number,
    bytes:string,
}